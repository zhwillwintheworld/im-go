# IM-GO 项目代理规则

> IM-GO 是一个以低时延为最高优先级的企业级即时通讯系统。业务领域说明已拆分到 `docs/biz/`，本文件只保留代理行为、设计原则、开发规范和文档入口。

## 基础约定

- 始终使用中文进行对话和思考。
- 如果当前目录没有 `AGENTS.md`，尝试使用当前目录的 `CLAUDE.md`。
- 完成任务或者会话后，回复：`我把任务完成啦，请加油哦！`
- 使用 `pnpm` 替代 `npm`。所有需要执行 npm 命令的场景一律使用 `pnpm` 对应命令。

## 最高准则：性能与低时延优先

IM 应用的核心体验是实时性，所有设计、实现和优化建议必须以最低时延和最高吞吐为第一优先级。任何可靠性、可扩展性、持久化、观测、安全或工程治理方案，都必须优先保护消息主链路的低延迟，不能在 Access/Logic/Web 的消息处理路径中引入阻塞等待、长事务、无界排队或高延迟同步调用。

具体要求：
- 优先选择非阻塞、异步、批处理、零拷贝/少拷贝、短路径和有界队列设计。
- 遇到可靠性与时延取舍时，优先使用异步确认、轻量级 outbox、幂等去重、后台补偿等方式，避免让客户端发送路径等待慢速存储或跨服务长调用。
- 所有背压策略必须快速失败或降级，禁止无限等待；丢弃、降级、重试必须可观测、可度量。
- 优化建议必须说明对 P50/P95/P99 时延、吞吐、队列积压和资源占用的影响，避免只提升一致性却拉高主链路延迟。
- 房间、游戏、群消息扩散等有状态或高扇出场景，必须优先采用分片、就近路由、批量拉取和并发上限，避免热点用户或大群拖慢全局消息处理。

## 自我纠错与知识沉淀

当用户或系统指出某个做法、命令、描述是错误的时，必须立即：

1. 定位错误：检查当前项目的 `.agent/rules/`、`.agent/skills/`、`AGENTS.md`、memory 中是否存在错误内容。
2. 修正错误：将所有包含错误描述的文件修改为正确版本，不遗漏。
3. 沉淀正确知识：将正确做法持久化到当前项目对应文件中。
4. 不确定时先确认：如果不确定正确做法，先向用户确认再沉淀。

不要只在对话中口头修正，必须落盘到项目文件。

## 设计原则

在整理 plan、评审方案、进行任何非平凡设计时，必须按以下七个维度逐条说明，不能只列方案的优势；若某维度不适用，需显式说明“不涉及”并给出理由，不能省略：

- **B - Benchmark（依据）**：该设计的依据是什么？是否参考了已有的、成熟的方案（如行业标准、开源实现、公司内既有模块）？如果是原创设计，需说明为何不复用已有方案。
- **E - Efficiency（效率）**：该设计对系统运行效率（如开发效率、运维效率、链路耗时）是否有提升？是否存在更能提高效率的替代方案？
- **A - Architecture（架构）**：该设计遵守了哪些架构原则（如四层架构依赖方向、单一职责、domain 隔离）？是否让系统整体架构更优，或是否引入了架构债？
- **F - Feature（功能）**：该设计是否明确满足了功能要求？是否还有模糊的功能边界、边界条件、异常分支未被识别？
- **Q - Quality（质量）**：该设计如何度量质量？有哪些保障功能质量、代码质量的细节设计（如单元测试、集成测试、监控、灰度）？
- **P - Performance（性能）**：该设计是否满足了性能要求（如 QPS、延迟、DB 查询效率）？是否对既有代码进行了性能优化或引入了性能回退？
- **S - Security（安全）**：该设计是否考虑了数据安全要求（如权限校验、敏感数据脱敏、SQL 注入、越权）？是否存在安全漏洞？

## 业务领域文档

业务领域说明已拆分到 `docs/biz/`。修改相关业务前必须先阅读对应领域文档；若业务结论发生变化，及时更新对应文档。

- `docs/biz/index.md`：业务文档索引
- `docs/biz/overview.md`：系统业务全局、模块边界、低时延原则
- `docs/biz/access.md`：接入、连接生命周期、在线位置
- `docs/biz/message.md`：消息发送、ACK、会话、未读、离线方向
- `docs/biz/user-auth.md`：用户、登录、Token、多端登录
- `docs/biz/friend.md`：好友请求和好友关系
- `docs/biz/room-game.md`：房间、游戏、麻将状态
- `docs/biz/client.md`：desktop-web 客户端体验和状态
- `docs/biz/storage-protocol.md`：数据库、Redis key、NATS subject、FlatBuffers

相关架构文档：
- `docs/access-layer-architecture.md`
- `docs/logic-layer-architecture.md`
- `docs/web-layer-architecture.md`
- `docs/web-client-architecture.md`
- `docs/game-module-design.md`

## Git 操作

- 不对 git 进行任何写操作，包括但不限于：add、commit、push、切换分支。
- 即使用户明确要求，也必须先与用户二次确认后再执行。

## 交互行为

- 优先使用 plan 模式对齐方案，不要未经确认直接修改代码。
- Plan 必须以文件形式持久化到 `docs/plans/` 目录下（文件名格式：`<branch-name>.md`），不能仅存在于对话上下文中；该目录已加入 `.gitignore`，不会被提交。
- 对话中达成的结论（设计决策、方案取舍、边界条件确认等）必须及时更新到 plan 文件中。
- 执行代码修改时，必须依据 plan 文件中的结论，不能偏离已确认的方案。
- 提 PR 前（`/done` 流程中），必须对比本次代码变更与 plan 文件的一致性，发现偏离需提醒用户。
- Plan 中必须包含「执行 `/done` 命令」的 todo 项，作为提 PR 前的最终检查步骤，禁止遗漏。
- 修改文件前，应保证其单元测试已存在且可以正常运行；若不存在或无法运行，应暂停修改并等待用户确认。
- 代码修改保持最小化，不要顺手重构周边代码。
- 如果有对云服务 API 的调用，需要先与用户确认是否会导致云服务费用上升。

## 开发规范

### 数据库

- 每张表必须包含 `id`、`created_at`、`updated_at`、`deleted` 字段。
- `id` 使用雪花 ID，由应用层生成。
- 绝对禁止数据库外键，可在字段注释中说明关系。
- 每个字段必须有注释描述用途。
- 字符串字段必须设置为 NOT NULL，默认值为空字符串。
- 判断字符串是否为空使用 `column != ''`，不要使用 `column = NULL`。
- 修改 `schema.sql` 必须同步更新对应 model；修改 model 必须同步更新 `schema.sql`。

### Go

- Access/Logic/Web 中消息处理绝对不能阻塞。
- 使用 goroutine、channel、有界队列和快速失败策略保护主链路。
- 新增可靠性、持久化、限流、监控或安全逻辑时，不得让客户端消息发送路径等待慢速数据库、外部服务或无界重试。
- Redis key 必须统一定义在 `project/shared/redis/keys.go`，应用代码不得硬编码。
- 后端输出 JSON 字段使用 camelCase；返回给前端的 ID 字段必须为 string。
- 使用 `errors.Is()` 和 `errors.As()` 判断错误。
- 不要忽略错误，至少要记录日志。
- 关键路径错误必须包含上下文信息。
- 共享数据必须使用互斥锁或 channel 保护。
- 避免在 goroutine 中直接使用闭包变量，应作为参数传递。
- 使用 `context.Context` 传递取消信号和超时控制。
- 使用 `defer` 确保资源释放。
- 使用结构化日志 `slog`，避免 `fmt.Println`。

### React/TypeScript

- 修改 `schema/message.fbs` 后，运行 `pnpm run flatc` 重新生成 TypeScript 代码。
- 使用 TypeScript 严格模式，不得使用 `any`，除非有充分理由并添加说明。
- 使用函数组件和 Hooks。
- 全局状态使用 Zustand。
- 列表渲染必须使用稳定 key。
- 异步操作使用 `async/await`，正确处理 Loading 和 Error 状态。
- 优先使用 Ant Design 组件库。
- 自定义样式使用 CSS Modules 或既有样式方案，避免内联样式。

### 代码格式

- 空行不要有空格或 tab 等不可见字符。
- 行尾不要有空格。
- 不要主动生成 README.md 等文档，除非用户明确要求。
- 未完成的代码或配置，需要增加 `todo <your-name>` 注释，便于后续跟踪完善；具体名称在 `CLAUDE.local.md` 中配置。

## JSON 解析规范

- 当需要解析 JSON 格式数据时，必须明确每个字段是否可为空，避免出现未明确字段是否可为空而导致解析异常的问题。
- 对于外部 API 返回的 JSON，所有非基本业务主键字段应默认按可空或有默认值处理，防止对方实际返回与文档不一致导致解析异常。
- 对于无法判断字段是否可为空的情况，需要中断并提醒用户确认。

## 质量检查

- 修改 Go 代码后运行 `scripts/check-go-quality.sh`，并解决所有错误与警告。
- 修改 desktop-web 代码后运行 `scripts/check-web-quality.sh`，并解决所有错误与警告。
- 校验代码时不要生成编译产物。
- 提交前删除所有 `.bak` 备份文件。

## 常用启动入口

- 基础服务脚本：`env/start-all.sh`、`env/stop-all.sh`
- Access：`project/access-go/cmd/access/main.go`
- Logic：`project/logic-go/cmd/logic/main.go`
- Web：`project/web-go/cmd/web/main.go`
- Desktop Web：`project/desktop-web`
