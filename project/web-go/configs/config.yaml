app:
  name: im-web
  port: 8082
  mode: debug  # debug, release, test

jwt:
  secret_key: "8Z9X7s2kQ8a9b7d6f5g4h3j2k1l0p9o8i7u6y5t4b8n9m0l1k2j3h4g5f6d7s8a9s7d6f5g4h3j2k="
  access_expire: 168h   # 7 days
  refresh_expire: 720h  # 30 days
  auto_renew_threshold: 4h  # 剩余时间小于此值时自动续期

database:
  host: localhost
  port: 5432
  user: postgres
  password: xhxxygwl
  name: im_db
  max_open_conns: 100
  max_idle_conns: 10
  conn_max_lifetime: 1h

redis:
  host: localhost
  port: 6379
  password: "xhxxygwl"
  db: 0
  pool_size: 100

cors:
  allowed_origins:
    - "http://localhost:8083"
    - "https://im.sudooom.com"
    - "*"  # 允许局域网设备访问
  allowed_methods:
    - GET
    - POST
    - PUT
    - DELETE
    - OPTIONS
  allow_credentials: true

rate_limit:
  enabled: true
  requests_per_minute: 60
